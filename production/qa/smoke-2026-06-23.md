## Smoke Check Report

**Date**: 2026-06-23
**Sprint**: Data/Balance Infrastructure Epic
**Engine**: Godot 4.6.3
**QA Plan**: Not found — run `/qa-plan sprint` before broader manual QA
**Argument**: sprint
**CI**: Configured at `.github/workflows/tests.yml`

---

### Environment

- Test directory: found at `tests/`
- Smoke checklist: found at `tests/smoke/critical-paths.md`
- Current sprint plan: not found
- Engine preferences: found at `.claude/docs/technical-preferences.md`
- Skill default Godot command: `godot --headless --script tests/gdunit4_runner.gd 2>&1`
- Effective project command: `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit/data --ignoreHeadlessMode 2>&1`

The skill default command was executed and returned exit code 1 because
`tests/gdunit4_runner.gd` is a deprecated wrapper that intentionally exits with
an instruction to use the GdUnit4 command tool. This is a tooling warning, not a
test failure.

---

### Automated Tests

**Status**: PASS (43 tests, 43 passing)

- Command: `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit/data --ignoreHeadlessMode 2>&1`
- Result: 43 test cases, 0 errors, 0 failures, 0 flaky, 0 skipped, 0 orphans
- Report: `reports/report_36/results.xml`
- HTML report: `reports/report_36/index.html`

Per-suite summary:

| Suite | Tests | Errors | Failures | Skipped |
|-------|-------|--------|----------|---------|
| `story_001_manifest_test` | 6 | 0 | 0 | 0 |
| `story_002_schema_validator_test` | 12 | 0 | 0 | 0 |
| `story_003_domain_cache_test` | 7 | 0 | 0 | 0 |
| `story_004_hot_reloader_test` | 6 | 0 | 0 | 0 |
| `story_005_tuning_knob_test` | 6 | 0 | 0 | 0 |
| `story_006_version_migrator_test` | 6 | 0 | 0 | 0 |

Expected negative-path logs appeared during the suite:

- Schema validation errors for intentionally invalid fixtures
- Missing-schema warnings for fallback behavior tests
- Deleted-domain fallback warning
- Unregistered tuning knob warning
- Incompatible version and failed migration rollback errors

These logs are asserted by the tests and did not produce test failures.

---

### Launch Smoke

**Status**: PASS (headless project launch)

- Command: `godot --headless --path . --quit 2>&1`
- Result: exit code 0
- Observed startup: DataManager loaded manifest, `damage_params`, and `tuning_knobs`; Godot MCP game helper registered capture logging.

This confirms the project starts in headless mode without a crash. It does not
prove that the interactive main menu, input navigation, or new-game flow works.

---

### Test Coverage

| Story | Type | Test File | Coverage Status |
|-------|------|-----------|-----------------|
| Story 001: ManifestLoader + 4 状态机 + 重试 | Logic | `tests/unit/data/story_001_manifest_test.gd` | COVERED |
| Story 002: SchemaValidator + 三级失败处理 | Logic | `tests/unit/data/story_002_schema_validator_test.gd` | COVERED |
| Story 003: DomainCache + 核心查询接口 + 懒加载 | Logic | `tests/unit/data/story_003_domain_cache_test.gd` | COVERED |
| Story 004: HotReloader 热重载机制 | Logic | `tests/unit/data/story_004_hot_reloader_test.gd` | COVERED |
| Story 005: TuningKnobRegistry 旋钮管理 | Logic | `tests/unit/data/story_005_tuning_knob_test.gd` | COVERED |
| Story 006: VersionMigrator 版本迁移 | Logic | `tests/unit/data/story_006_version_migrator_test.gd` | COVERED |

**Summary**: 6 covered, 0 manual, 0 missing, 0 expected.

---

### Manual Smoke Checks

The required manual batches were not interactively confirmed in this session.
Items below are recorded as unverified unless automated evidence directly covers
them.

- [x] Godot project starts headless without crash — PASS
- [-] Game launches to main menu without crash — NOT CONFIRMED
- [-] New game / session can be started from the main menu — NOT CONFIRMED
- [-] Main menu responds to all inputs without freezing — NOT CONFIRMED
- [-] Player movement / jump / camera follow — NOT IMPLEMENTED OR NOT CONFIRMED
- [-] Player attack and damage numbers — NOT IMPLEMENTED OR NOT CONFIRMED
- [-] Dodge with i-frames — NOT IMPLEMENTED OR NOT CONFIRMED
- [-] Save game completes without error — N/A, save gameplay path not verified in this smoke
- [-] Load game restores correct state — N/A, load gameplay path not verified in this smoke
- [-] 60 fps target on hardware — NOT CHECKED
- [-] 5-minute memory growth — NOT CHECKED

---

### Missing Test Evidence

All Logic and Integration stories in the Data/Balance Infrastructure epic have
test coverage.

---

### Advisory Items

- `tests/gdunit4_runner.gd` is deprecated but still matches the smoke-check skill's default command. Either update the skill/project runner command or replace the wrapper with a working passthrough before relying on `/smoke-check` automation unmodified.
- No QA plan was found under `production/qa/qa-plan-*.md`. Generate one before a full manual QA hand-off.
- Manual gameplay critical paths remain unconfirmed because this session only verified the Data/Balance slice and headless startup.

---

### Verdict: PASS WITH WARNINGS

Automated Data/Balance tests passed and all six Data/Balance stories have test
coverage. The build is suitable for the next implementation step and for
targeted manual QA of this slice, but it is not a clean full-game smoke pass
because the interactive main menu, new-game flow, input response, gameplay
movement/combat, save/load, and performance checks were not manually confirmed.
