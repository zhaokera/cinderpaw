# QA Evidence: Old Factory Return Checkpoint

> **Story**: Player Abilities Story043 — Old Factory Return Checkpoint
> **Date**: 2026-06-30
> **Engine**: Godot `4.7.stable.official.5b4e0cb0f`
> **Scene**: `res://scenes/factory_route_transition_shell.tscn`

## Verdict

PASS. The Old Factory return checkpoint is present in the runtime scene, uses
image-generated transparent checkpoint art, stays unavailable before the return
patrol is defeated, becomes available afterward, records a scene-local
savepoint, and feeds non-boss death respawn selection.

## Automated Tests

Focused RED:

- `reports/report_952/` failed `old_factory_return_checkpoint_test.gd` after
  the Story043 acceptance test was expanded. Expected failures were missing
  checkpoint diagnostics and activation APIs.

Focused GREEN:

- `reports/report_954/` passed `old_factory_return_checkpoint_test.gd` `3/3`
  with `0` errors, failures, flaky tests, skipped tests, or orphans.

Related regression:

- `reports/report_955/` passed `old_factory_return_patrol_ambush_test.gd`
  `3/3`.
- `reports/report_956/` passed `story_004_savepoint_respawn_selection_test.gd`
  `4/4`; this suite still emits known Godot cleanup-time ObjectDB/resource
  warnings after passing.
- `reports/old_factory_return_checkpoint_related_service_lift_handoff.log`
  passed `old_factory_service_lift_handoff_test.gd` `2/2`.
- `reports/old_factory_return_checkpoint_related_service_lift_exit.log` passed
  `old_factory_service_lift_scene_manager_exit_test.gd` `2/2`.
- `reports/report_957/` contains the final service-lift XML report generated by
  the last of those two service-lift commands; the separate command logs above
  preserve both suite results.

## Asset Pipeline

Generated visual asset:

- Source:
  `assets/generated/source/old_factory_return_checkpoint_imagegen_20260630.png`
- Alpha source:
  `assets/generated/source/old_factory_return_checkpoint_alpha_20260630.png`
- Runtime PNG:
  `assets/environment/old_factory_return_checkpoint/old_factory_return_checkpoint.png`
- Metadata:
  `assets/generated/source/old_factory_return_checkpoint_imagegen_20260630.json`

The source image was generated through image generation, chroma-key processed to
transparent alpha, cropped/padded, resized to a 256x256 transparent runtime
prop, imported by Godot 4.7, and recorded in
`design/assets/asset-manifest.md` and `design/assets/entity-inventory.md`.

## Headless Smoke

`reports/old_factory_return_checkpoint_factory_scene_smoke.log` exited `0`.
Targeted keyword scan found no project script, parse, invalid-call,
invalid-access, missing-resource, or resource-load failures. The run still emits
the project's known Godot cleanup-time `ObjectDB` / `resources still in use`
messages at exit.

## MCP Runtime Probe

Godot MCP session ran the Factory route scene with Godot `4.7-stable
(official)` and `autosave=false`.

Runtime checks confirmed:

- `FactoryReturnCheckpoint` exists in the live scene tree.
- The node is in group `savepoint` and uses
  `res://src/feature/savepoint_runtime.gd`.
- `Visual` uses
  `res://assets/environment/old_factory_return_checkpoint/old_factory_return_checkpoint.png`.
- Initial locked state: `visible=false`, `available=false`,
  `activated=false`; activation returns `false`.
- After return patrol defeated: `visible=true`, `available=true`, prompt
  `Repair Savepoint`, ids
  `old_factory_return_checkpoint / area_03_factory / return_checkpoint`.
- Activation returns `true`, records last checkpoint with scene
  `area_03_factory` and spawn `return_checkpoint`, and updates RouteLabel to
  `Factory Savepoint Secured`.
- `get_last_discovered_savepoint()` exposes the same checkpoint for
  `GameFlowController` non-boss respawn selection.
- Game logs contained only MCP helper registration; editor logs were empty
  after the final runtime probe.

Visual evidence:

- `reports/visual/cinderpaw-mcp-old-factory-return-checkpoint-20260630.png`
  is a non-empty screenshot showing the checkpoint, prompt, route label, reward
  cache, and service lift in the Old Factory return route.
